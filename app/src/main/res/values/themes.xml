<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Minimal M3 theme: colorAccent mat use karo -->
    <style name="Theme.TransportDocsManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional: status bar tweak -->
        <item name="android:statusBarColor" tools:targetApi="21">@android:color/transparent</item>
    </style>
</resources>
